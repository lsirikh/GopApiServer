# 명세 ↔ 코드 ↔ Swagger 3자 정합 정밀감사

- **감사 일시**: 2026-08-07
- **방법**: 멀티에이전트 2단 워크플로우 — 1차 21에이전트(10도메인 3자대조 + 도메인별 적대적 반증), 2차 5에이전트(파일 직접읽기 종합)
- **소스**: 코드 `app/`(진실 소스) / 명세 `GOP_Restful_Api_연동설계.md`(16.7K줄) / **라이브 Swagger 실덤프**(251 오퍼레이션·292 스키마, info.version 6.3.2)
- **원칙**: 추정 금지 — 모든 finding 은 code file:line + 명세 line + swagger 경로/스키마 실인용 필수, 반증관이 재검증

---

# 명세 최신화 실행목록 (통합본)

## 3자 정합 판정

| 지표 | 값 |
|---|---|
| **총 점검수** | **977건** (A 기반 183 · B 장비/이벤트 236 · C 통합/서버 115 · D 계정/리포트 443) |
| 원 findings | 114건 (A 31 · B 36 · C 23 · D 24) |
| **확증 갭(중복 제거 후)** | **103건 — P0 14 · P1 50 · P2 39** |
| 검증 상태 | CONFIRMED 85 · PARTIAL 11 · **[미검증] 7** · REFUTED 제외 0 |
| 축 분포 | 명세 오류 68 · Swagger 미노출/오문서 17 · 코드 결함 12 · 코드-명세 불일치 6 |

**최대 문제 3가지**

1. **RBAC 집행이 양방향으로 붕괴 (P0 3건, 현 배포에서 실발생)** — `EnumPermissionModule`에 `integrations`/`files`가 없어 PERMISSION_MAP 16개 쓰기 라우트가 **비-ADMIN 영구 403**(매트릭스 저장 자체가 422)인 동시에, §7 서브리소스 12개 쓰기 경로와 §5 ROI/XyPoint/그룹멤버십 10경로는 PERMISSION_MAP 미등록 + `MATRIX_DENY_MODE=off`로 **전면 무검사 통과**. 같은 리소스에서 "전부 막힘 / 전부 열림"이 공존.
2. **Swagger 응답 계약이 사실상 비어 있음** — 2xx 빈 스키마 **71/251(28.3%)**, 422가 실응답과 다른 `HTTPValidationError`로 **234건** 오문서, 401·403·500·503 응답 **0건**, reports 라우터 `response_model` **0건**(미등재 스키마 9종). 클라이언트 codegen이 전 도메인에서 무력화.
3. **명세가 코드보다 낡고 자기모순 (ChangeLog만 최신)** — 본문 미문서 엔드포인트 4건 + §13.1 부록 미등재 19건, 에러코드표·enum 개수·인가 서술이 구버전. 그중 **§8.6.4 `before_date`(명세) vs `older_than_days`(코드)** 는 명세대로 호출 시 파라미터가 무시되어 **기본 30일치 메트릭이 비가역 전량 삭제**되는 파괴적 오계약.

---

## 실행목록 (§ 순서)

| § | 항목 | 축 | 구체 지시 | P | 검증 |
|---|---|---|---|---|---|
| §3.1/§3.2 | 성공 응답 `meta.request_id` 항상 null | 코드-명세 불일치 | `ResponseMeta(` 인스턴스화 0건·주입 코드 0건(`common.py:48-51` default None). `middleware/request_id.py:28,34`는 `request.state`와 X-Request-ID 헤더만 세팅. 응답 직렬화 시 meta 주입하거나 명세 L210-213·Swagger example(UUID)을 "null 가능 — 추적은 X-Request-ID 헤더"로 정정 | P1 | CONFIRMED |
| §3.2 | raw-dict 반환 경로 `meta` 통째 누락 **43건** | 코드-명세 불일치 | `auth.py`(685,786,917,1243)·`users.py`(137,154,233,335,427,457,569,604,699,847,924,1015,1082,1145)·`user_groups.py`(53,98,168,279,352,413,453)·`user_sessions.py`(125,249,277,344,387,523)·`grants.py`(139,161,225,237,282)·`enclosure_metrics.py`(95,223,272,321,370)·`settings.py`(53,100)을 `ApiResponse[...]`/`ApiSingleResponse[...]` 반환으로 통일. **`event_mapping_cameras/lamps/speakers` 21건은 제외**(반증: response_model 지정으로 default_factory가 meta 자동 주입) | P1 | PARTIAL |
| §3.2 | 2xx 응답 **71건**이 빈 스키마 `{}` (251 중 28.3%) — 하위 도메인 행의 우산 항목 | Swagger 미노출 | POST /api/auth/login·logout·refresh, GET /api/auth/me/permissions, /api/users(+me,+password,+photo), /api/user-groups, /api/system-events, /api/reports/*, /api/devices/enclosures/{id}/metrics 등에 `response_model=ApiResponse[...]`/`ApiSingleResponse[...]` 명시. 정상 대조군 `servers.py:525`, `server_metrics.py:250-252` | P1 | CONFIRMED |
| §3.2/§6.8/§12 | 에러 응답 전역 미선언 — 401·403·500·503 Swagger 0건, `ApiErrorResponse` 참조 0 | Swagger 미노출 | openapi 응답 집계 = 200×220,201×30,202×1,400×1,404×13,409×1,422×234. 코드는 401×15·403×10·409×20·503×1(`tracking.py:214`)·500×1을 실제로 던짐. `common.py:100 ApiErrorResponse`를 공통 `responses={401,403,404,409,500}`로 FastAPI 전역 등록. **§6.8 7라우트 400/404 개별 땜질 금지** — 42개 라우터 중 7개만 `responses=` 선언이 전역 관행이므로 공통 스키마+기본값 도입으로 일괄 처리 | P1 | CONFIRMED / PARTIAL |
| §3.3 | 204 No Content 행 미사용 / 202 누락 | 명세 오류 | L262 삭제 또는 "DELETE 성공도 200 + `ApiSingleResponse(data=null)`"로 정정(`reports.py:873` 주석이 의도 명시, 204 라우트 0건). **202 Accepted 행 추가**(`reports.py:703`, openapi 202×1) | P2 | CONFIRMED |
| §3.2/§7/§12 | 타임스탬프 예시 `...Z` (실제 `+09:00`) | 명세 오류 | L211·L231·L251·L15986·L16022·L16039 + §7 전역 **21줄**(10200-10201, 10210-10211, 10271-10272, 10346-10347, 10417-10418, 10476-10477, 10658, 11042-11043, 11784-11785, 12137-12138, 12333-12334)을 `+09:00`으로 일괄 치환해 §3.4 L281과 일치. 코드 `main.py:601 to_display(utc_now()).isoformat()`는 aware라 `Z` 불가 | P2 | CONFIRMED |
| §4.1 | `EnumDeviceCategory`에 `LAMP` 누락(명세 5 vs 코드·Swagger 6) | 명세 오류 | **4곳**: L366-372 블록에 `LAMP="lamp"  # 경광등(v3.4)`, L380 산문 괄호, **L1093 §5 폴리모픽 Discriminator 목록**, L1077-1086 JTI 다이어그램에 lamps 자식 테이블. 헤더는 `(v2.0 신규, v2.4·v3.4 확장)` | P1 | CONFIRMED×2 |
| §4.2 | `EnumDetectionType`/`EnumFaultType` **요청** 필드가 str → Swagger enum 미노출 | 코드 결함 | `schemas/event.py:80 result: str`, `:245 reason: str`를 각 Enum으로 복원(응답 str 유지 가능) → `detections.py:400-405`·`malfunctions.py:415-420` try/except 제거. 승격 근거: ChangeLog L16711 `response_schema_audit`가 "요청 스키마는 strict 유지"를 못박음 = 정책 이탈. 예시 라인 L7584·L8178 | P2 | CONFIRMED |
| §4.4 | `EnumServerType (25종)` docstring ↔ 실멤버 26 | 코드 결함(주석) | `enums.py:230` → `(26종)`(소분류 주석 합 8+4+4+6+3+1=26과 자기모순). 명세 L568 헤더에도 `(26종)` 명기 | P2 | CONFIRMED |
| §4.6 | `EnumAuditActionType` 명세 18종 ↔ 코드 21멤버(+raw 2종) | 명세 오류 + 코드 결함 | L686 제목 개수 정정 + L713 앞에 `GRANT_CREATED`/`GRANT_REVOKED`/`GRANT_EXPIRED`(enums.py:509-512) 블록 추가, `enums.py:481` docstring `(18종)`→`(21종)`. **개수 21 vs 23은 결정 필요**(users.py:522/:559 raw `USER_PHOTO_CHANGED`/`USER_PHOTO_DELETED` enum 승격 여부). Swagger는 enum 제약 없는 str + 미등재 → 명세가 유일 값 사전. **응답 str 완화는 정정 대상 아님**(L14896-14900 NOTE) | P1 | CONFIRMED×2 |
| §4.7 | `EnumConfigResourceType (19종)` docstring ↔ 실멤버 21 | 코드 결함(주석) | `enums.py:546` → `(21종)`, L553-555 카테고리 주석에 `SETTINGS`·`SUPPRESSION_SCHEDULE` 추가. 값 집합은 명세 L750·Swagger와 21값 정합 | P2 | CONFIRMED |
| §4.8/§10.4.2 | `EnumReportPeriod` `custom` 누락(명세 4 vs 코드·Swagger 5) | 명세 오류 + Swagger 오문서 | 명세 L825 `(5종)` + `CUSTOM="custom"`. **`enums.py:752` docstring `(4종)`→`(5종)` 수정해야 Swagger description 모순(enum 5개인데 "4종")이 함께 해소.** §10.4.2 L15648 `period_type ... (7d,30d,90d,1y)` 및 Request Body 예시(L15633-15640) start_date/end_date 부재도 동시 갱신 | P1 | CONFIRMED×2 |
| §4.8/§10.4.3 | `EnumReportStatus` `CANCELLED` 3개 소스 전부 부재 | 명세 오류 + Swagger 오문서 + 코드 결함(문구) | 명세 L840 `(5종)` + `CANCELLED`(명세 전체 grep 0건), §10.4.3 status 설명 추가. **`reports.py:795` Query description 문자열부터 5종으로 수정**(그대로 Swagger 전파) | P1 | CONFIRMED×2 |
| §4.8 | `EnumChartType` dead enum + 응답 str 완화 미선언 | 명세 오류 | ① `grep EnumChartType app/` 정의 1·사용 0인데 L855-866이 4종 유효 계약 + "Preview Chart.js 렌더링"으로 허위 서술(실제 `schemas/report.py:245 type: str`) → 채택하거나 §4.8·enums.py 양쪽 삭제. ② §4.5/§4.6/§4.8 정의 블록에 "응답에서는 str 완화 — 신규 값은 본 목록으로 제한" 주석 추가 | P2 | PARTIAL |
| §4.10(신설)/§6.8 | `EnumSuppression*` 4종의 §4 정의 부재 + 끊어진 상호참조 | 명세 오류 | **두 안 상충 — 결정 필요.** (A) §4.10 신설: TargetType(device/group/all)·Side(detection/surveillance/both)·EventScope(connection/detection/malfunction/all)·Status(pending/active/expired/cancelled), 파생 우선순위는 L16640에 이미 있으므로 **이관·링크만**. (B) §4 신설 없이 L9997 문구를 "(Enum 값은 아래 표 참조 — Python 정의: app/utils/enums.py)"로 1줄 수정(값 4종은 L10003/10005/10007/10009에 인라인, 정보손실 0). forward-ref는 L9997, target_side 행은 L10006 | P2 | PARTIAL×2 |
| §4 카탈로그 | 코드 51종 중 8종이 §4에 정의 블록 없음 | 명세 오류 | 차집합 = SystemEventType(343)·SystemEventSeverity(388)·PermissionModule(842)·PermissionVerb(858)·Suppression 4종(147/154/165/173). §4.10~4.12 신설 또는 도입부에 "SystemEvent는 §8.7.1, RBAC는 §9 참조" 인덱스 추가. **서술 주의: L293/L295는 완전성 선언이 아님** → 결함은 "§6.8 L9997이 §4를 참조하는데 대상이 없다"로 좁혀 기술 | P2 | CONFIRMED |
| §5·§6·§7 | 인가(필요 권한) 서술이 3개 대형 섹션 전체에 부재 | 명세 오류 | 문서 전체 "필요 권한" 문자열 0건, §5·§6(1053~10143) `grep 권한` 0건 — 권한 서술은 §9·§10 일부뿐. **수정 범위 = §7.1 표 신설 단독이 아니라 `permission_map.py` 전체(≈100여 항목)를 각 섹션 권한 컬럼/부록 표로 1:1 매핑** | P2 | PARTIAL(범위확대) |
| §5 | ROI·XyPoint·그룹멤버십 **10경로**가 PERMISSION_MAP 미등록 → 무검사 통과 | 코드 결함 + 명세 오류 | ROI 쓰기 4(`rois.py:191/267/348/444`), XyPoint 쓰기 3(`xypoints.py:100/167/231`), 그룹멤버십 3(`device_groups.py:705/793/899`)를 **PERMISSION_MAP에 10행 추가**(라우터 데코레이터 아님 — 설계 원칙). **원 주장 대폭 축소: speakers/enclosures/lamps/device_groups/camera_presets는 이미 보호됨. `enclosure_metrics` POST/DELETE는 permission_map.py:87-89의 의도된 머신 인제스트 제외 → 대상 아님** | P1 | PARTIAL |
| §5.3.1 | 목록 예시만 `user_password: "********"` | 명세 오류 | L2441을 `"user_password": "password123"`으로 정정해 §5.3.2(L2537)와 통일. 목록/단건 모두 CameraResponse 단일 스키마·마스킹 로직 없음 | P2 | CONFIRMED |
| §5.3.2/§5.6 | `GET /devices/cameras/{id}`, `GET /devices/groups/{id}` Swagger 응답 `{}` | Swagger 미노출 | `cameras.py:291`에 `response_model=ApiSingleResponse[CameraWithPresetsResponse]`, `device_groups.py:148`에 그룹 상세 스키마 부착 후 재빌드·Swagger 재덤프. **정정: CameraWithPresetsResponse는 dead 아님(`cameras.py:336`에서 인스턴스화) — 문제는 components 미등록 1건** | P1 | CONFIRMED |
| §5.4.4 | Speaker PATCH 표의 `type_device`(SpeakerUpdate에 필드 없음 → 무증상 폐기) | 명세 오류 | L3790 행 삭제 + "type_device는 생성 시에만 지정, PUT(§5.4.5)은 SpeakerCreate 사용이라 유지 가능" 명시. **동일 행이 Camera PATCH 표 L2830에도 존재 → CameraUpdate 확인 후 §5.3.4 동시 판정(후속)** | P1 | CONFIRMED |
| §5.5.1/5.5.3/5.5.5 | `threshold_config` 하위 키 3종 오기 | 명세 오류 | L4121-4127 등 예시를 코드 6키로 교체: temp_high 40.0 / temp_low -10.0 / humidity_high 80.0 / current_high 10.0 / voltage_low 200.0 / vibration_high 70. **실패모드 정정: `vibration_threshold` 전송 시 경보 미발생이 아니라 조용히 폐기되고 기본값 70 저장되는 무증상 오설정.** L4224/L4423 생성·수정 예시(2키)도 정합화 | P1 | CONFIRMED |
| §5.5.4 | Enclosure PATCH 예시/표의 `door_status`·`type_device` | 명세 오류 + Swagger 오문서 | 명세 L4315·L4330 예시, L4341(type_device)·L4345(door_status) 표 행 삭제 + "door_status 변경은 `PATCH /enclosures/{id}/status` 전용" 명기. **추가로 `enclosures.py:321` docstring `- **door_status**: 도어 물리적 상태` 삭제**(Swagger description 오염). EnclosureUpdate는 extra="forbid"라 방치 시 422 | **P0** | CONFIRMED |
| §5.5.9~§5.5.12 | Enclosure Metrics 4 라우트 response_model 부재(진짜 dead 스키마) | Swagger 미노출 + 코드 결함 | `enclosure_metrics.py` L43/L177/L230/L279에 부착 — POST는 threshold_exceeded 포함 래퍼 신설, 목록/latest는 `ApiResponse[list[EnclosureMetricResponse]]`/`ApiSingleResponse[...]`, DELETE는 `ApiSingleResponse[None]`. import만 된 EnclosureMetricResponse/LatestResponse 실사용 0건 확인 | P1 | CONFIRMED |
| §5.5.12 | DELETE metrics 응답 `data.deleted_count` | 명세 오류 | L4934-4940을 `"message":"Deleted 150 metrics for enclosure 1", "data": null`로 정정 + "삭제 건수는 message에 보존(v4.8 envelope 표준)" 주석. message 문자열도 코드와 상이 | P2 | CONFIRMED |
| §5.5.13 | `GET /api/enclosure-metrics` 계약 전면 불일치 | 명세 오류 + Swagger 미노출 | 명세 L4957-4961 쿼리 5종·L4966-4980 `data.items/total`+`pagination`을 코드 실제 shape(`data[]={enclosure_id, enclosure_name, latest_metrics}`, 쿼리 0개)로 정정. **페이징이 실요구면 라우터 구현 — 결재 필요**(v4.6 Phase 6 M04 미해소분) | **P0** | CONFIRMED |
| §5.7.3 | Preset `is_restricted_zone` 필드표·예시 누락 | 명세 오류 | 필드표에 `| is_restricted_zone | boolean | N | 감시금지구역 표시(기본 false) |` 추가, §5.7.1~5.7.5 응답 예시에 `"is_restricted_zone": false` 삽입(VMS/NVR/db_monitor/Central UI 4개 매니저 참조 필드) | P2 | **[미검증]** |
| §5.8.3 | `ROICreate` 필수 컬럼 역전 | 명세 오류 | L6328-6331의 resolution_width/height/points 필수 N→Y, points에 `최소 3개(min_length=3)`, 기본값 1920.0/1080.0 표기 삭제. **강도 보정: 요청 예시(L6309-6322)는 4필드를 다 담고 있어 422는 표의 'N'을 믿고 생략한 경우로 한정** | P1 | CONFIRMED |
| §5.9.1 | `GET /rois/{id}/points` 응답 키 `points`(명세) vs `items`(코드) + 쿼리 미기재 | 명세 오류 | L6556-6564를 `"data":{"items":[...],"total":N}`로 정정, 각 item에 created_at/updated_at 추가. **동시에 Query Parameters 표(page 기본1 / limit 기본100 le=100)와 pagination 블록 신설** | **P0** | CONFIRMED(중복 지시 병합) |
| §5.11 | `LampResponse.user_password` "보안상 제외" 서술 | 명세 오류 | L7221 문구 삭제, §5.11.1/5.11.2 응답 예시에 `"user_password": "lamp1234"` 추가 + "v4.10 Phase 1 평문 응답 정책" 각주. 코드는 평문 노출이 확정 정책(`device.py:1053-1054`) | P1 | CONFIRMED |
| §6.1 | SYNC_DETECTION NATS 발행 계약이 §6 본문 미기재 | 명세 오류 | §6.1 서두에 'NATS 발행' 소절 신설: subject `all.sync.detection`, from=DBApi, cmd=SYNC_DETECTION, action=UPDATED\|DELETED, body `{cmd,action,resource_id}`(알림형), **POST(INSERT) 미발행**, best-effort(EXCEPTION WHEN OTHERS·원 트랜잭션 미롤백). §6.8.9 포맷 준용. 근거 `app/db_triggers.py:519-542` | P1 | CONFIRMED |
| §6.1.1 | `type_event` 쿼리 파라미터 부재 | 코드-명세 불일치 | **A안 권장**: `detections.py:198`에 `type_event: Optional[str]=Query(None)` + L232 필터에 `DetectionEvent.type_event == type_event`. B안은 명세 L7367·L7375 예시 URL에서 제거. malfunctions=reason, connections=필터없음이 이미 정합이라 A가 대칭 → **결정 필요** | P1 | CONFIRMED |
| §6.1.1/§6.2.1/§6.3.1 | `start_date/end_date` required 표기 + 순서검증 422 예시 | 명세 오류 | L7364-7365·L7975-7976·L8556-8557을 `optional(미지정 시 전 기간)`으로 정정, L7458-7471·L8053-8066·L8637-8650의 `end_date must be greater than start_date` 422 예시 삭제(코드 전역 미검증). §6.4.2 "v4.6 정정 — 모두 optional" 표현 차용 | P1 | CONFIRMED |
| §6.1.1~§6.1.7 | detail 예시에 `frame_width/frame_height` 전무 | 명세 오류 + 코드 example 결함 | 명세 JSON 예시 6곳(L7411-7419, 7516-7524, 7572-7593, 7659-7676, 7738-7762, 7931-7934)에 `"frame_width":1920,"frame_height":1080`을 signal 다음 줄에 추가. **`DetectionEventUpdate.detail`의 json_schema_extra example도 frame_height 누락 + Malfunction 키(first_*/second_*) 혼입 → 코드 example 자체 정리** | P2 | CONFIRMED |
| §6.1.3/§6.2.3/§6.3.3/§6.4.1 | POST Create 스키마만 `extra='forbid'` 부재 | 명세 오류 | **조치 역전(P1→P2)**: 코드(ignore)↔Swagger(additionalProperties 미설정)는 정합이고 명세는 침묵 = 계약 파손 아님. 1순위는 코드 강화가 아니라 **명세 4곳에 "현재 POST는 미정의 필드를 무시하며 action_reported는 서버가 False로 강제" 1줄 명기**(회귀 리스크 회피) | P2 | PARTIAL |
| §6.1/§6.2 | `DetectionDetail`/`MalfunctionDetail` 정의만 되고 미사용 | 코드-명세 불일치 | (A) Create/Replace/Update/Response의 `detail`을 `Optional[DetectionDetail]`/`Optional[MalfunctionDetail]`로 교체(+`extra="allow"`) → Swagger 구조 노출·오타 차단, (B) 최소조치로 L7356·L8044-8051에 "detail 내부 무검증 자유형 JSONB" 명기 후 미사용 클래스 2종 삭제 → **결정 필요** | P2 | CONFIRMED |
| §6.1~§6.4 | 응답 message 문자열 3계열 불일치 | 명세 오류 | 코드가 진실 소스 — L7769/8356/8893/9370 `updated`→`replaced successfully`, DELETE 예시 L7833(및 8404·8944·9430)→`"Detection event 1002 deleted successfully"` 형태, 목록 L7388/7998/8577/9081→건수 없는 고정 문구 | P2 | **[미검증]** |
| §6.2.5 | Malfunction PUT `action_reported` "전송해도 무시됨" | 명세 오류 | L8335·L8350을 "전송하면 **422**(extra=forbid)"로 통일. §6.1.5(detections PUT)·§6.3.5(connections PUT)에도 동일 주의문 추가(round-trip PUT 즉시 실패) | **P0** | CONFIRMED |
| §6.4.1 | POST 요청 `created_at` 명세 미기재 | 명세 오류 | L8997-9005 Request Body에 `"created_at": "2026-01-14T10:20:00" //(optional, 미입력 시 서버 시각)` + 필드표 신설. "PUT/PATCH는 created_at 변경 불가(422) — 조치 시각 영구 보존(Phase 12-7d)" 비대칭 정책을 §6.4.4/6.4.5에 명시 | P1 | CONFIRMED |
| §6.4.2 | `start_date/end_date` "미지정 시 1년 전 기본값" 서술 | 명세 오류 | A안 권장 — L9062-9063의 '미지정 시 1년 전/현재 시각' 삭제 → '미지정 시 전 기간'. detections/malfunctions/connections도 기본값 없음이라 A가 일관 | P2 | **[미검증]** |
| §6.4.5 | Action PUT 요청 `from_event_id`(v4.6 "4필드 required" 오정정) | 명세 오류 | L9337-9367 Request Example·Body·v4.6 주석에서 `from_event_id` 삭제 → type_event/content/user 3필드 required. §6.4.4/§6.4.5에 "from_event_id·**created_at** 전송 시 422(extra=forbid)" 주의문. v6.3 spec_freshness_audit P0 PUT 정정이 actions만 누락한 잔여분 | **P0** | CONFIRMED |
| §6.8 | 요청 필드 `recurrence_rule` 미기재 + 미구현 | 명세 오류 | 필드 요약표(L9999-10011)에 `| recurrence_rule | string(255) | | Phase 2 RRULE 예약 — 저장만 하고 반복 평가 안 함(단발 창) |` 추가 + §6.8.2에 미구현 경고. **정정: L10038 응답 예시엔 이미 `"recurrence_rule": null` 존재 → 결함은 (a)필드표 누락 (b)요청 필드/제약 미기술 (c)미구현 경고 부재** | P1 | CONFIRMED |
| §6.8 | 응답 `status` 타입(명세 enum / 코드 str / Swagger 자유문자열) | 코드-명세 불일치 | `schemas/event_suppression.py:102`를 `status: EnumSuppressionStatus`로 변경(반환값이 str-Enum `.value`라 그대로 통과) → 재빌드 후 $ref 확인. **하향 사유: 런타임 값 집합은 enum 4종과 완전 일치, 실피해는 codegen 타입 분기뿐** | P2 | PARTIAL |
| §6.8.6 | PATCH 400 에러 명세 미기재 | 명세 오류 | L10059 Error를 `400(대상 device/group id 미존재 — 배열 원소 전부 검증) · 404 · 422(end≤start, device→ids≥1/group→ids≥1)`로 정정. 근거 `event_suppression_schedules.py:310/318 → :112/:122` | P2 | CONFIRMED |
| §6.8.10 | 억제 202 응답이 3개 이벤트 POST Swagger에 미노출 | Swagger 미노출 | `detections.py:~365`·`malfunctions.py:~380`·`connections.py:~350`에 `responses={202:{... example {success,suppressed,message,schedule_id}}}` + docstring Error 절 202 기재. 명세가 PidsProxy/AiAnalysis에 "202=성공, 재시도 금지"를 요구하는데 codegen은 미정의로 처리 → 억제 창 중 재전송 폭주 위험 | P1 | CONFIRMED |
| §7 | 서브리소스 단건 PATCH·PUT·DELETE + `.../bulk` **12개 쓰기 경로 인가 무검사 통과** | 코드 결함 + 명세 오류 | `permission_map.py` Integrations 블록에 12항목 추가: `("POST","/api/integrations/event-mappings/{}/cameras/bulk"):("integrations","edit")`, `("PATCH"/"PUT",".../{}/cameras/{}"):("integrations","edit")`, `("DELETE",동일):("integrations","delete")` — lamps/speakers 각 4종. 근거: `matrix_enforcer.py:126-127` 정규화 후 미등록 → `:128-138` `MATRIX_DENY_MODE="off"`(config.py:52)로 return, 라우터 가드 0건 | **P0** | CONFIRMED |
| §7/§9.4.7 | PERMISSION_MAP의 `integrations`·`files` 모듈 키가 `EnumPermissionModule`에 부재 → **16개 쓰기 라우트 비-ADMIN 영구 403** | 코드 결함 | `permission_map.py:116-126`(integrations 10)·`:128-134`(files 6) vs `enums.py:842-855`(12종 미포함). `schemas/user.py:47,49` extra=forbid + `Dict[EnumPermissionModule,...]`로 매트릭스 저장 422, `auth.py:128-131 _role_group_allows` 문자열 조회라 영구 False, grant 경로(`_effective_allows_async:1030-1042`)로도 우회 불가, `matrix_enforcer.py:149-150` ADMIN bypass만 통과. **(A) enums.py에 `INTEGRATIONS`/`FILES` 추가(12→14) + 그룹 매트릭스 마이그 + §9.4.7/Swagger 재덤프, 또는 (B) 기존 어휘(setup_feature 등)로 재배정 → 결정 필요.** ADMIN 전용이 의도면 §9.4.7에 '매트릭스 부여 불가' 명시. 해당 라우터(`event_mappings.py:21`, `event_mapping_cameras.py:40`, `file_groups.py:23`, `thumbnails.py:33`)에 route-level RBAC 전무 + `.env:9 AUTH_MODE=token` → **현 배포에서 실제 발생 중** | **P0** | CONFIRMED×3 |
| §7.3.1/§7.5.1 | 서브목록 응답 봉투 불일치 | 명세 오류 | §7.5.1(12099-12147) Response를 §7.5.9(12302-12337)와 동일한 `"data":{"items":[...],"total":N}`로 교체 + pagination 블록 삭제. **§7.3.1(10635-10643)은 `data.{items,total}` 뒤 잉여 `pagination` 블록 삭제** (§7.4.1은 정합) | **P0** | CONFIRMED |
| §7.3.1/§7.4.1/§7.5.1 | `page`/`limit`(+§7.5.1 `lamp_id`/`is_enable`) 명세만 존재 | 명세 오류 | 10551-10554·11315-11318·12091-12096 Query 표 삭제 → "페이지네이션 없음(전건 반환)". **10654-10668·11379-11397의 `"page must be a positive integer"` 422 예시 블록도 함께 삭제**(검증 자체가 없어 절대 미발생) | P1 | CONFIRMED |
| §7.3.9/§7.4.9/§7.5.10 | bulk — 요청 내 동일 id 중복이 어느 배열에도 미분류 | 명세 오류 + 코드 결함(주석) | 명세 11154를 "v4.6 FR-5 이후 **첫 건만 처리, 나머지는 무시**(어느 분류 배열에도 미포함)"로 정정. **`event_mapping_cameras.py:665-677` 주석이 'skipped_config_ids로 분류'라 적혀 있으나 실제는 append 없이 `continue` → 주석 수정.** lamps/speakers 포함 3곳 일괄 | P1 | CONFIRMED |
| §7.3.9 | bulk `failed_items` 예시 vs 필드 정의표 자기모순 | 명세 오류 | 11128-11136 예시의 `failed_items` 원소를 preset 사유(`"Target preset with id 999 not found"`)로 교체, `camera_id 999`는 `"not_found_config_ids":[999]`로 이동 → 11154-11155 정의표(코드와 일치)와 일치. §7.4.9·§7.5.10 동일 예시 동시 점검 | P2 | CONFIRMED |
| §7 | 서브리소스 **21개 라우트** `response_model=ApiSingleResponse[dict]` → 응답 필드 0 노출 | Swagger 미노출 | 단건 GET/POST/PATCH/PUT은 `ApiSingleResponse[EventMappingCamera\|Speaker\|LampResponse]`, 목록 3종+독립목록 3종은 items/total 래퍼로 교체(bulk 6개는 정상 대조군). **`EventMappingCameraListResponse`(schemas/integration.py:207)는 `cameras.py:25` dead import, `:190 EventMappingCameraNestedResponse`는 전 코드 미참조 → 활용/제거 동시 결정** | P1 | CONFIRMED |
| §7.5.3 | `POST .../lamps` 중복 매핑 시 명세 409 vs 실제 500 | 코드 결함 | `event_mapping_lamps.py:214` 이후 벌크와 동일한 `(event_mapping_id, lamp_id)` 사전 조회 → `HTTPException(409, "...already exists")`. `uq_event_mapping_lamp`(models/integration.py:288)는 있으나 IntegrityError 핸들러 없음 → `main.py:686` general handler가 500+SEC-04 마스킹. **Swagger responses는 `['201','422']`로 409·404 둘 다 미노출.** cameras/speakers 단건 POST 동일 결함 점검 | P1 | CONFIRMED |
| §7.5.5 | PUT 요청 본문 참조 스키마명 `EventMappingLampCreate` 오기 | 명세 오류 | 12238을 `EventMappingLampReplace — 8필드 전부 필수(기본값 없음)`로 고치고 §7.5.3 형식 필드 표 추가. **축소 사유: 12238이 이미 "전체 필드 포함"이라 422 시나리오 미성립 → P1→P2.** 별건: **카메라 PUT만 `EventMappingCameraCreate` 재사용(필수 camera_id 1개) → PUT semantics 불일치** 별도 검토 | P2 | PARTIAL |
| §8.2.1 | `type_server` 필터가 명세에 없음 | 명세 오류 | 12669-12673 Query 표에 `| type_server | string(EnumServerType) | N | 서버 유형 필터 |` 추가. **원시 문자열 완전일치라 Enum 외 값·오타는 422가 아니라 빈 배열(0건)**임을 동작으로 명시 | P1 | CONFIRMED |
| §8.2.6 | DELETE category 응답 `data:{id:10}` vs 실제 `data:null` | 명세 오류 | 12999-13008을 `"message":"Server category 10 deleted successfully","data":null`로 정정. **`server_categories.py:402` docstring "삭제된 카테고리 ID"도 동시 정리** | P1 | CONFIRMED |
| §8.3.6 | DELETE server 응답 `data:{id:1}` vs 실제 `data:null` | 명세 오류 | 13476-13485를 `"message":"Server 1 deleted successfully","data":null`로 정정. **`servers.py:545` docstring "Response: 삭제된 서버 ID"도 정리(명세 오기의 출처로 추정)** | P1 | CONFIRMED |
| §8.3.7 | 명세만 있는 `severity`/`acknowledged` 쿼리 필터 | 명세 오류 | 13515-13516 삭제 또는 대체 안내 — **"§8.7.1 계열 `/api/system-events` 사용, 파라미터명은 `is_acknowledged`(13871)"**(§8.7.2는 상세 조회, 절 번호 오기 주의) | P1 | CONFIRMED |
| §8.3.7 | `limit` 기본값 명세 50 vs 코드·Swagger 20, 상한 100 미기재 | 명세 오류 | 13514를 `| limit | integer | N | 20 | 페이지당 항목 수(최대 100) |`로 정정. 동일 오기가 4958행(§5.5.x)에도 존재 → 후속 | P1 | CONFIRMED |
| §8.3.7 | 응답 envelope — 명세 top-level `pagination` vs 코드 `data.pagination` | 명세 오류 | 13518-13544를 `data:{items,total,pagination:{page,limit,total_pages}}`로 재작성(servers.py:303-316). `ApiSingleResponse_dict_`에 top-level pagination 부재, `meta`(timestamp/request_id)도 대체 불가 | P1 | CONFIRMED |
| §8.3.7 | 아이템 필드명/구성 + message 불일치 | 명세 오류 | 13525-13535를 servers.py:283-297 키 집합으로 교체(`acknowledged`→`is_acknowledged`, 누락된 server_id/title/detail/acknowledged_by/acknowledged_at 추가). **message도 상이 — 명세 13521 `"System events retrieved successfully"` vs 코드 305 `"Server system events retrieved successfully"`. `source`/`updated_at`은 "본 엔드포인트는 두 키를 내려주지 않는다"로 기술.** 후속: `ApiSingleResponse[dict]`(servers.py:236) 전용 스키마 승격 | P1 | CONFIRMED |
| §8.6 | `collected_at` 저장형 — 명세 "KST 벽시계 naive" vs 실제 aware-UTC(timestamptz) | 명세 오류 + 코드 결함(명명) | 13658을 "aware/naive 모두 허용, 서버가 **UTC aware**로 정규화해 `timestamptz` 저장, 응답은 DISPLAY_TZ(+09:00)"로 정정. **ChangeLog 16682도 동일 오서술 → 함께 정정 필수.** `server_metrics.py:29-35` 낡은 docstring + 함수명 `_to_naive_kst`(실체 `to_utc`) → `_to_utc_aware` 류로 정리(동작 무변경) | P2 | CONFIRMED |
| §8.6.1 | POST metrics message — 명세 `recorded` vs 코드 `created` | 명세 오류 | 13714를 `"message": "Server metrics created successfully"`로 정정 | P2 | CONFIRMED |
| §8.6.2 | 명세 `start_date/end_date` vs 코드·Swagger `start_time/end_time` | 명세 오류 | 13747-13748을 `start_time`/`end_time`으로 정정(287 규약과 일치) + **"레코드 생성시각(`created_at`) 기준, 닫힌구간" 명시**(288-291) | P1 | CONFIRMED |
| §8.6.2/§8.6.4 | 필터·정렬·삭제 기준 컬럼이 `created_at`(≠`collected_at`)인데 미문서화 | 명세 오류 | 양 절에 "필터·정렬·삭제 기준은 적재 시각 `created_at`(수집 시각 `collected_at` 아님)" 1줄 명시. 기준을 collected_at으로 바꾸는 것은 계약 변경 → 별도 PRD | P2 | CONFIRMED |
| §8.6.4 | `DELETE /api/servers/{id}/metrics` — 명세 `before_date`(datetime) vs 코드·Swagger `older_than_days`(int, 기본 30) | 명세 오류 | 13797-13800 Query 표를 `| older_than_days | integer | N | 삭제 기준 일수(기본 30, 최소 1) |`로 교체. **명세대로 `?before_date=` 호출 시 FastAPI가 무시 → 기본 30일 전량 비가역 삭제.** **ChangeLog 16741(v2.9)의 "before_date 필터" 서술도 함께 정정**, 삭제 기준 컬럼이 `created_at`임을 명시. §5.5.x Enclosure(4924·4945)가 실제 before_date 계약 = 복붙 흔적 | **P0** | CONFIRMED |
| §8.6.4 | DELETE metrics 응답 — 명세 `data:{deleted_count:150}` vs 코드 `data:null` | 명세 오류 | 13802-13809를 `"message":"Deleted 150 old metrics for server 1","data":null`로 정정. Swagger `ApiSingleResponse_NoneType_`의 data는 `{"type":"null"}` 고정 | P1 | CONFIRMED |
| §8.7 | System Events 7개 라우트 response_model 미부착 → Swagger 응답 전면 `{}` | Swagger 미노출 + 코드 결함 | `system_events.py` 데코레이터 7곳(73/133/202/229/278/323/364)에 `ApiResponse[list[SystemEventResponse]]`/`ApiSingleResponse[SystemEventResponse]`/`ApiSingleResponse[SystemEventSummary]`/`ApiSingleResponse[None]` 부착, `_system_event_to_response()`→`SystemEventResponse.model_validate(event)` 대체(dead 스키마 2종 해소) | P1 | CONFIRMED |
| §8.7 | 예시에 존재하지 않는 `type_event` 소문자 값 **6곳** | 명세 오류 | L13527(§8.6)·13885·13920 `threshold_warning`→`RESOURCE_THRESHOLD`, L13944·13973 `custom`→실존 값(예 `SYSTEM_UPDATE`), L14071-14074 by_type 키 → §8.7.1 표의 대문자 15종 내. POST 예시 `custom`은 실제 422 유발 | P1 | CONFIRMED |
| §8.7.2 | `GET /api/system-events` pagination 미반환(null) | 코드 결함 | `system_events.py:160-199`에 `count_stmt = select(func.count())` + 동일 필터로 total 계산, `detections.py:281-292` 패턴대로 `PaginationMeta(page,limit,total,total_pages)`를 ApiResponse에 실어 반환 | P1 | CONFIRMED |
| §9.1 | 중앙 매트릭스 집행 모델이 명세 본문에 전무 | 명세 오류 | §9.1.1 '인가 집행 모델' 신설 — (1) `AUTH_MODE != "token"` 휴면(matrix_enforcer.py:120), (2) PERMISSION_MAP 등록 경로만 판정, (3) role=ADMIN bypass(:149-150), (4) 미등록 경로 `MATRIX_DENY_MODE` off(기본, config.py:52)/observe/enforce 3모드(:127-139), (5) 공개 allowlist 전체: prefix `/docs`,`/redoc`,`/openapi`,`/health`,`/static`,`/favicon` + exact `/` + `POST /api/auth/login`,`POST /api/auth/refresh`(:27-29). 현재는 ChangeLog L16629·16638·16705·16707·16711-16713에만 산재 | P2 | CONFIRMED |
| §9.2.1 | `X-Client-Id` 헤더 Swagger 미선언 + login docstring client_id 누락 | Swagger 미노출·오문서 | auth.py login 시그니처에 `x_client_id: str \| None = Header(None, alias="X-Client-Id", description="클라이언트 식별자(우선). 패턴 ^[A-Za-z0-9._:-]{1,64}$, 위반 시 무시")` 선언(:582가 이 값 우선 사용), docstring Request Body에 `client_id`(선택) 추가. 명세 :14313-14314는 이미 정확 | P2 | CONFIRMED |
| §9.3.1 | users 표 '권한' 열 8행 전부 `**ADMIN**` | 명세 오류 | :14486-14495 권한 열 교체 — 목록/상세=`users:view`, 생성/수정/reset-password=`users:edit`, 삭제=`users:delete`, lock/unlock=`users:control`(모두 role=ADMIN bypass). :14505 산문이 정반대라 표↔산문 모순. **:14502/:14503(관리자 사진)은 이미 정합 → 손대지 말 것** | P2 | CONFIRMED |
| §9.3.2/§9.3.3 | `photo_url: null` 응답 예시 | 명세 오류 | **축소분만 채택**: 응답 예시 2곳(:14535, :14583) `null`→`"/api/users/photo/default.png"`, :14509 default-fill 서술을 §9.3.2 각주로 재게시. **요청 예시 :14564는 null 유지.** 감사관 P1→P2 하향(‌:14509가 이미 default-fill 계약 명시, Swagger는 `anyOf[string,null]`이라 필연) | P2 | PARTIAL |
| §9.2.5/§9.4.1/§9.5.1/§9.9.5 | 절 배치 오류 + /me 예시 photo_url 누락 + 표 인가 열 부재 | 명세 오류 | ① :15320 §9.9.5 블록을 :15312 `---`(§10 시작) **앞**으로 이동. ② §9.2.5 예시(:14439 employee_number 다음)에 `"photo_url": "/api/users/photo/default.png",` 삽입. ③ §9.4.1 표 인가 열(목록/상세/소속사용자=`user_groups:view`, 생성/수정/삭제/permissions=`role=ADMIN`), §9.5.1 표 인가 열(목록/상세=`users:view`, DELETE /{id}·/user/{id}=`users:control`, /me·/me/{id}=인증만). **§9.4.1 :14611은 이미 "(ADMIN 전용)" 표기 → 예외** | P2 | CONFIRMED |
| §9.4.7 | `EnumPermissionModule` 명세 8종 vs 코드·Swagger 12종 | 명세 오류 + 코드 결함(문구) | :14647 "8종 모듈 키"→"12종", :14669 목록에 `map`,`broadcast`,`setup_system`,`setup_feature` 추가, :14722 "(8종)"→"(12종)"·라인 `enums.py:779`→`:842`. **`schemas/user.py:50 modules` Field description도 8종 나열 → Swagger PermissionsSchema에 그대로 노출되므로 코드 1곳 동시 수정**(문서 3 + 코드 1). §4에 EnumPermissionModule/Verb 정의 블록 신설. **성격: 클라 무지가 아니라 명세 자기모순**(ChangeLog :16716은 12종, §9.8 도입부 :15182는 setup_system을 운영 계약으로 사용 — 규범 2행만 stale) | P1 | CONFIRMED×3 |
| §9.4.7 | 구현 위치 라인번호 5개 stale + `time_restriction` 미문서 + Tag 오기 | 명세 오류 | :14720-14724를 user_groups.py:285 / user.py:40 / user.py:25 / enums.py:842 / enums.py:858, 의존성 `require_admin_async`로 정정. Request Body에 `time_restriction: Dict(선택, ⚠ 서버 미집행 — UI 메타데이터)` 행 추가(Swagger PermissionsSchema 3필드 vs 명세 2필드). :14728 Tag `user-groups`→`User Groups`. **권고: 라인번호 대신 파일::심볼 표기**(`app/utils/enums.py :: EnumPermissionModule`)로 재발 차단 | P2 | CONFIRMED |
| §9.8.1 | 엔드포인트 표 '인가' 열이 `ADMIN` | 명세 오류 | L15188/L15189를 `setup_system:view`/`setup_system:edit`(ADMIN bypass)로 정정. 같은 절 L15182는 이미 정확 = 내부 모순. 코드 `settings.py:46,56 require_perm_async("setup_system", …)` | P2 | CONFIRMED |
| §9.8.3 | PUT 403 사유를 '비-ADMIN'으로 서술 | 명세 오류 | L15247을 `403 — setup_system:edit 미보유(메시지: Insufficient permission: requires setup_system:edit)`로 정정(발생부 `auth.py:1176-1180`). 부가로 Swagger PUT responses가 {200,422}뿐이라 403 미정의 | P2 | CONFIRMED |
| §9.8.3 | `SessionSettingsUpdate` extra 미차단 → 오타 키가 조용히 200 | 코드 결함 + 명세 오류 | `schemas/settings.py:31`에 `model_config = ConfigDict(extra="forbid")`(현재 파일 전체 model_config 0건 = 기본 ignore). 대조군 `grant.py:14`는 forbid, Swagger도 GrantCreate=`additionalProperties:false`와 비대칭. 핸들러 `settings.py:66-100`은 전 필드 None이면 `changed_keys=[]`로 200 반환. 명세 L15231을 `(SessionSettingsUpdate, extra 금지)`로 보강 | P2 | CONFIRMED |
| §9.4.7/§9.8/§9.9 | `SessionSettingsResponse`·`GrantResponse`·`UserGroupResponse` 응답 스키마 Swagger 전무 | Swagger 미노출 | `settings.py:46,56`의 `response_model=None`을 `ApiSingleResponse[SessionSettingsResponse]`로, `grants.py:64/142/164/244` 4라우트 + `user_groups.py:23,59,104,174,285,358,420` 7라우트에 `responses={200:{"model": ApiResponse[List[UserGroupResponse]]}}` 등 문서 전용 지정(런타임 dict 반환 불변, 선례 `user_sessions.py:91/:353`). components 292개에 세 스키마 부재. 명세는 L15191-15229(12필드)·L15288-15302으로 이미 규정 | P1 | CONFIRMED×2 |
| §9.9.2 | POST /users/{id}/grants 응답 예시에 `user_login_id`/`user_name` 누락 | 명세 오류 | **정정 대상은 L15291 1곳** — §9.9.3(L15306)엔 JSON 예시가 없으므로 "필드 구성 = §9.9.5와 동일" 1줄만 보강. `grants.py:43-61 _serialize()`가 POST/GET 공용이고 `:118-120`에서 `grant.user`/`grant.group` 첨부로 값이 실제 채워짐 | P2 | CONFIRMED |
| §9.9.3/§9.9.5 | grant 조회 인가가 `(ADMIN)`/`403 (require_admin)`으로 잔존 | 명세 오류 | L15306 `(ADMIN)`→`(users:view — ADMIN bypass 또는 매트릭스)`, L15358 `403 (require_admin)`→`403 (users:view 미보유)`. 같은 §9.9 표 L15262/15264·L15326은 이미 users:view = 내부 모순. 코드 `grants.py:142,164` | P2 | CONFIRMED |
| §9.9.5 | GET /api/grants의 page/size 경계가 Swagger 파라미터에 미표기 | Swagger 미노출 | `grants.py:166-167`을 `page: int = Query(1, ge=1)`, `size: int = Query(20, ge=1, le=100)`로 변경해 OpenAPI minimum/maximum 자동 반영(현재 `{"type":"integer","default":1}`만). `:192-194` 수동 422 검증은 메시지 호환 위해 유지 가능. 명세 L15332-15334는 이미 정확 | P2 | CONFIRMED |
| §9.3.x | users 라우터 7개 성공 응답 스키마 미바인딩 | Swagger 미노출 | `users.py:83/:143/:160/:576/:610/:705`에 `responses={200:{"model":ApiResponse[List[AccountUserResponse]]}}` / `ApiSingleResponse[AccountUserResponse]` 지정. 대조: `/api/auth/me`만 $ref 정상 | P2 | CONFIRMED |
| §10.1 | 개요 표 '보고서 컴포넌트 15종'(실제 5카테고리 21종) | 명세 오류 + 코드 결함(주석) | :15325 → '(5개 카테고리 21종)'. 같은 문서 :868·§10.2.2는 이미 21종 = 자기모순. 병행 **`reports.py:170` docstring '4개 카테고리: SUMMARY, DEVICE, EVENT, SYSTEM' → '5개 카테고리: …, USER'** | P1 | **[미검증]** |
| §10.2.2 | components 응답의 `label`(카테고리)·`chart_type`(컴포넌트) 미문서 | 명세 오류 | :15386-15448 예시 각 카테고리에 `"label": "장비"`, 각 컴포넌트에 `"chart_type": "PIE"\|"BAR"\|"LINE"\|null` 추가 + null=그리드·카드형 주석. **response_model 부착 시 ComponentInfo/ComponentCategoryResponse 필드명이 category→name, name→label로 어긋나 스키마 정정 선행 필요** | P2 | **[미검증]** |
| §10.3.2 | templates 목록 응답이 상세 응답 복사본 | 명세 오류 | :15466-15488 예시에서 `components` 배열·`updated_at` 제거 → `"component_count": 3`(reports.py:144 `_template_to_list_response()`), "목록=경량/상세=전체" 명시. 라우터에 `ApiResponse[List[ReportTemplateListResponse]]` 부착(스키마는 이미 코드와 일치) | P1 | CONFIRMED |
| §10.3.4 | templates `name` 1~100자 / `description` 최대 500자 제약 미집행 | 코드 결함 **또는** 명세 오류(택1) | 권장: `schemas/report.py:44-56`을 `name: str = Field(..., min_length=1, max_length=100)`, `description: Optional[str] = Field(None, max_length=500)`로 강화 → Swagger maxLength 자동 노출. 제약 의도가 없다면 §10.3.4 표의 '(1~100자)'·'(최대 500자)' 삭제. **어느 쪽이든 3자 일치 필수 → 결정 필요** | P1 | **[미검증]** |
| §10.3.6 | DELETE 응답 `data:{id}` vs 실제 `data:null` + message 불일치 | 명세 오류 | 예시를 `{"success":true,"message":"Report template 1 deleted successfully","data":null}`로 정정(reports.py:426-430). §10.4 DELETE /generations/{id} 신설 시에도 동일 형태로 기재해 두 삭제 계약 일치 | P2 | **[미검증]** |
| §10.4.1/§13.1 | reports 미문서 엔드포인트 **4건** | 명세 오류 | §10.4.1 표(:15619-15626, 현 6행)에 4행 추가 + §10.4.7~10.4.10 신설 — **cancel**(`reports.py:901`, reports.delete/200 `{id,status,task_cancelled}`/400(이미 COMPLETED·FAILED·CANCELLED)/404), **DELETE /generations/{id}**(:859, reports.delete/data:null/PDF best-effort), **detail.csv**(:1113, reports.view/필수 `type=detection\|malfunction\|action\|system\|config\|audit\|login\|session`/text-csv UTF-8 BOM/400(미완료)·400(unknown type)·404 3분기·`Content-Disposition: report_{id}_{type}.csv`(:1172)), **preview/{id}**(:1275, reports.view/text-html/`mode=full\|compact`). §13.1 부록에도 4줄 동시 추가. ※DELETE·preview/{id}는 ChangeLog v5.4(:16713) 산문에만 존재 → "계약 절 미기재"로 표현. **`grep -c response_model= app/routers/reports.py` = 0**이라 ReportGenerationResponse도 Swagger 미등재 | **P0** | CONFIRMED×2 |
| §10.4.2 | `POST /reports/generate` start_date/end_date 누락 + period_type 필수 표기 역전 | 명세 오류 | 필드표(:15644-15653)에 start_date/end_date 2행 추가(둘 다 지정 시 period_type 무시하고 `"custom"` 저장, end_date 00:00→23:59:59.999999 확장), period_type 필수 '예'→'**조건부(start+end 미지정 시 필수)**'(Swagger required=[report_type,title]). Error 절에 422 3종(편측 지정·end<start·span>`REPORT_MAX_RANGE_DAYS`). ※자정 확장 시맨틱은 §3 :287에 이미 산문 존재 → 갭은 3건으로 한정 | P1 | CONFIRMED |
| §10.4.3 | generations 응답 4필드 무계약 | 명세 오류 + 코드-스키마 불일치 | `reports.py:454 _generation_to_response()`가 /generate·/generations·/generations/{id} 응답에 progress 3키 + `preview_html_url`(항상) + 조건부 `pdf_download_url`을 실음. §10.4.3 예시(현 13키)에 5필드 명문화(progress_pct int 0~100, progress_stage `start\|setup\|master_data\|html\|pdf\|done\|null`, progress_updated_at datetime\|null). **`schemas/report.py:336 ReportGenerationResponse`에도 5필드 없음(대신 미반환 pdf_file_path/pdf_file_size/summary_data/error_message 보유) → 스키마 정정 후 response_model 부착** | P1 | CONFIRMED |
| §10.4.4 | download 410 `PDF_FILE_MISSING` 미문서 + Content-Disposition 오기 | 명세 오류 + Swagger 오문서 | 명세 `report_{id}_{date}.pdf` → 실제 `attachment; filename="report_{id}.pdf"; filename*=UTF-8''{quote(title)}.pdf`(reports.py:1012-1015). Error 절에 404(레코드 없음/pdf_file_path null)·410(`{error_code:"PDF_FILE_MISSING",message,report_id}`, 재생성 유도)·400(미완료). Swagger responses는 ['200','422']뿐 + 200이 application/json 오표기 → 라우터에 `responses={200:{"content":{"application/pdf":{}}},400,404,410}` 선언 | P1 | CONFIRMED |
| §10.4.5 | `GET /generations/{id}/preview` 응답 본문 구조 정반대 | 명세 오류 + 코드-명세 불일치 | :15729-15756의 `summary_data`/`created_at`/`completed_at` 삭제, 코드 실반환(reports.py:1236-1248) 7키 = id,title,report_type,period_type,start_date,end_date,**sections[{name,title,charts[{id,title,type,data{labels,values,datasets,colors}}],grids[{id,title,columns,rows,total_rows}],summary_data}]**로 교체. **`response_model=ApiResponse[ReportPreviewResponse]` 부착 전에 `schemas/report.py:267`에 `report_type` 추가**(현 스키마에 없어 잘림), 스키마에만 있는 generator_name/generator_department는 미반환 → 정리 선행 | **P0** | CONFIRMED |
| §10.5 | preview-page 'Swagger 미포함' 오기 + 307 미문서 + Jinja2 서술 stale | 명세 오류 | :15795 '개발용 HTML(Swagger 미포함)' 삭제 → '인증 필수(reports.view), Swagger 정식 노출'. §10.5.2 Response를 '**307 Redirect → GET /api/reports/preview/{id}**'(reports.py:1251, include_in_schema=False 없음)로 정정. §10.5.3 신설(text/html, `mode=compact\|full`). 'Jinja2 `app/templates/reports/preview.html` 기반' → `report_html_renderer.render_report_html_async`. ※preview.html·Jinja2Templates는 **존재하나 참조 0건 = 사문화** → "파일 없음"이 아니라 "렌더 경로 아님" | P1 | CONFIRMED |
| §10 전역 | reports 라우터 `response_model` **0건** → 응답 계약 Swagger 0% 노출 | Swagger 미노출 + 코드 결함 | 전 17 라우터에 부착(/templates→`ApiResponse[List[ReportTemplateListResponse]]`, /templates/{id}·POST·PATCH→`ApiResponse[ReportTemplateResponse]`, /generations·/generations/{id}·/generate→`ApiResponse[ReportGenerationResponse]`, preview→`ApiResponse[ReportPreviewResponse]`). `:49/:51` dead import 해소. **순수 JSON은 16이 아니라 14건**(download·detail.csv는 실제 pdf/csv인데 Swagger가 application/json 오표기), **미등재 스키마 최소 9종**(+EnumReportStatus, EnumReportComponent, ComponentInfo, ComponentCategoryResponse) | **P0** | CONFIRMED |
| §12.1 | 422 응답이 `HTTPValidationError{detail:[{loc,msg,type}]}`로 문서화(**234건**) — 실응답은 `{success,message,error{code,details[]},meta}` | Swagger 오문서 | openapi 251 오퍼 중 422 문서화 234 = HTTPValidationError $ref 230 + 빈스키마 2 + ValidationErrorResponse 2. `app/main.py` FastAPI() 생성부에 `responses={422:{"model":ValidationErrorResponse}}` 전역 지정(또는 openapi() 후처리 치환), `schemas/common.py:134-148 ValidationErrorResponse`에 `meta: ResponseMeta` 추가(현 props=success,message,error / required=[error]). 실핸들러 `main.py:672-683` | **P0** | CONFIRMED |
| §12 | 405 및 미매칭 404가 커스텀 엔벌로프를 타지 않음 | 코드 결함 | `main.py:16`이 `fastapi.exceptions.HTTPException`을 import하고 `:607`에 등록 → Starlette 라우팅이 올리는 `starlette.exceptions.HTTPException`은 MRO 조회에 안 걸려 기본 핸들러로 떨어짐(`fastapi.HTTPException is starlette.HTTPException == False`). 핸들러를 `starlette.exceptions.HTTPException`에도 등록 | **P0** | PARTIAL 파생(승격 권고) |
| §12.1 | `error.field_errors` 유령 필드 + `error.details` 타입 3분기 | 명세 오류 | `grep field_errors app/` = 0건 → L15981-15983 삭제, L16019 예제 문구 교체. `error.details`를 `null(HTTPException) \| dict(SESSION_REVOKED) \| ValidationFieldError[](422)`로 경로별 명시, 422는 `error.message`가 없고 최상위 `message:"Validation error"`(main.py:676)임을 규정. §12.3 400 예제(L16013-16025)를 실제 422 페이로드로 교체 | P1 | CONFIRMED |
| §12.2 | 422 에러코드 = `UNPROCESSABLE_ENTITY`(명세) vs `VALIDATION_ERROR`(코드·Swagger) | 명세 오류 | L16003을 `| 422 | VALIDATION_ERROR | 검증 실패·비즈니스 로직 오류 |`로 정정(근거 `main.py:578,622`, 라우터 422 HTTPException 37건 전부 code 미부여 — 전 코드베이스 `self.code=`는 `exceptions.py:26 SESSION_REVOKED` 1건). 400행(L15997)과 중복 안 되게 '400=BAD_REQUEST만'으로 재정리 | P1 | CONFIRMED |
| §12.2 | 에러코드 표 전체 정합 | 명세 오류 | **삭제/미구현 표기**: L16005 `DB_ERROR`(grep 0), L16007 `TIMEOUT/504`(grep 0), L15997 `400 VALIDATION_ERROR`. **추가**: `UNKNOWN_ERROR`(미매핑 폴백 — `auth.py:451` 429, `reports.py:1004` 410) + **429·410 행**. **추가 금지**: 502 BAD_GATEWAY(던지는 곳 0건인 죽은 매핑), 405(핸들러 미경유 → 위 코드 결함으로 처리) | P1 | PARTIAL |
| §13.1 | Swagger 251 오퍼 중 부록 미등재 **19건**(역방향 유령 0건) | 명세 오류 | 일괄 추가 — Report 4(cancel/DELETE/detail.csv/preview{id}), Grants 4(GET /api/grants, DELETE /api/grants/{id}, GET·POST /api/users/{id}/grants), Integrations mapping-{cameras,lamps,speakers} 3, Settings session 2, /api/auth/me/permissions, **event-suppression bulk-delete**(L16210 다음 줄 `` - `POST /api/event-suppression-schedules/bulk-delete` - 취소·종료 스케줄 일괄 하드삭제 (events:delete)`` — §6.8.1 표 L9994엔 이미 존재 = 내부 불일치), Logs 2(GET /api/logs, /api/logs/viewer), GET /health, GET / → Logs·health·root는 '운영/시스템 Endpoints' 소절 신설. **본문 미문서 확정 6건 = health, logs, logs/viewer, detail.csv, reports/preview/{id}, cancel**(`GET /`는 판정 보류). 재발방지: swagger_ops 덤프 대비 부록 diff를 5중 싱크 체크리스트에 상설화 | P1 | CONFIRMED×2 |

---

## 재작업 금지 (반증으로 제외·축소된 것)

- `event_mapping_cameras/lamps/speakers` **21개소 meta 누락** → 오탐(response_model로 meta 자동 주입).
- **502 BAD_GATEWAY 에러표 추가** → 금지(죽은 매핑). **405 에러표 추가** → 금지(코드 사안).
- **Account/Audit/Report 응답 enum str 완화를 결함으로 보고** → 금지(L14896-14900 NOTE + ChangeLog L16711의 의도적 트레이드오프). 남는 건 `EnumChartType` 1건.
- **Suppression 파생 매핑이 코드에만 존재** → 오탐(L16640에 전문 존재, 이관만).
- **§5 speakers/enclosures/lamps/device_groups/camera_presets RBAC 무보호** → 오탐(중앙 `main.py:451 Depends(enforce_matrix)`로 이미 보호). `enclosure_metrics` POST/DELETE는 **의도된 제외**(permission_map.py:87-89).
- **§7.5.5 PUT 422 실패 시나리오** → 미성립(스키마명 오기만 남음, P1→P2).
- **Tracking(§11) 전 항목** 무결(엔드포인트 3/3·쿼리 9/9·스키마 13/13,7/7,3/3), Swagger `props=(EMPTY)` 36건도 전부 enum 타입 스키마로 **정상** → 조치 없음. **§8.8 proxy-settings** 3자 완전 정합.
- 수치 정정: 빈 스키마 2xx **75→71**, meta 누락 **52→43**, HTTPValidationError **232→234**, reports 순수 JSON **16→14**, §5 RBAC 무보호 **다수→10경로**.

---

## [코드 결함] — 명세가 아니라 코드를 고쳐야 하는 것 (런타임 버그)

| # | 위치 | 내용 | P |
|---|---|---|---|
| 1 | `app/utils/enums.py:842-855` | `INTEGRATIONS`/`FILES` 부재 → PERMISSION_MAP 16개 쓰기 라우트가 비-ADMIN 영구 403(매트릭스 저장 422). **현 배포 실발생** | P0 |
| 2 | `app/security/permission_map.py` Integrations 블록 | 서브리소스 단건 PATCH/PUT/DELETE + bulk **12경로 미등록** → `MATRIX_DENY_MODE=off`로 인가 무검사 통과 | P0 |
| 3 | `app/main.py:16,607` | `starlette.exceptions.HTTPException` 핸들러 미등록 → 405·미매칭 404가 `{"detail":...}`로 엔벌로프 이탈 | P0 |
| 4 | `permission_map.py` | ROI 쓰기 4(`rois.py:191/267/348/444`)·XyPoint 쓰기 3(`xypoints.py:100/167/231`)·그룹멤버십 3(`device_groups.py:705/793/899`) **10경로 미등록** | P1 |
| 5 | `event_mapping_lamps.py:214` | 중복 매핑 사전 조회 부재 → UNIQUE 위반이 IntegrityError 핸들러 없이 **500**(409여야 함). cameras/speakers 단건 POST 동일 여부 점검 | P1 |
| 6 | `system_events.py:160-199` | 목록 pagination 미계산 → 항상 null | P1 |
| 7 | `schemas/settings.py:31` | `SessionSettingsUpdate` extra 미차단 → 오타 키가 조용히 200 | P2 |
| 8 | `schemas/event.py:80,245` | 요청 필드 `result`/`reason`이 str → Swagger enum 미노출 + "요청 스키마 strict" 정책 이탈 | P2 |
| 9 | `schemas/report.py:267,336` | `ReportPreviewResponse`에 `report_type` 누락(응답 잘림), `ReportGenerationResponse`에 progress 5필드 누락 + 미반환 4필드 보유 → response_model 부착 전 정정 | P1 |
| 10 | docstring/주석/명명 일괄 | `enums.py:481`(18→21)·`:546`(19→21)·`:230`(25→26)·`:752`(4→5); `reports.py:795` Query description 4종·`:170` 카테고리 4→5; `event_mapping_cameras.py:665-677` 오도 주석; `server_metrics.py:29-35` docstring + `_to_naive_kst`→`_to_utc_aware`; `servers.py:545`·`server_categories.py:402` "삭제된 ID"; `enclosures.py:321` door_status; `schemas/user.py:50` 8종 description | P2 |
| 11 | dead 코드 | `reports.py:49/51` dead import, `cameras.py:25 EventMappingCameraListResponse` dead import, `schemas/integration.py:190 EventMappingCameraNestedResponse` 미참조, `EnumChartType` 사용처 0, `templates/reports/preview.html`+Jinja2Templates 사문화 | P2 |

## [Swagger 전용] — Swagger 축 문제

| # | 내용 | 조치 지점 | P |
|---|---|---|---|
| 1 | 422가 `HTTPValidationError`로 **234건** 오문서(실응답과 완전 상이) | `main.py` FastAPI(responses={422:ValidationErrorResponse}) 전역 + `common.py:134-148`에 meta 추가 | P0 |
| 2 | reports 라우터 `response_model` **0건**, 미등재 스키마 9종, download/detail.csv가 application/json 오표기 | `reports.py` 17 라우터 | P0 |
| 3 | 2xx 빈 스키마 **71/251(28.3%)** | 우산 항목 — 아래 4·5·6·7이 실체 | P1 |
| 4 | §7 서브리소스 **21 라우트** `ApiSingleResponse[dict]` | `event_mapping_{cameras,lamps,speakers}.py` | P1 |
| 5 | §8.7 System Events **7 라우트** response_model 미부착 | `system_events.py` 73/133/202/229/278/323/364 | P1 |
| 6 | Enclosure Metrics **4 라우트** + `cameras/{id}` + `device-groups/{id}` | `enclosure_metrics.py` 43/177/230/279, `cameras.py:291`, `device_groups.py:148` | P1 |
| 7 | Settings 2 + Grants 4 + UserGroups 7 + Users 7 라우트 응답 스키마 부재(`SessionSettingsResponse`/`GrantResponse`/`UserGroupResponse` components 전무) | `settings.py:46,56`, `grants.py:64/142/164/244`, `user_groups.py`(7), `users.py`(6~7) | P1~P2 |
| 8 | 401·403·500·503 응답 **0건**, `ApiErrorResponse` 참조 0 / §6.8 400·404 미선언 | 전역 `responses` 기본값 도입(개별 땜질 금지) | P1 |
| 9 | 억제 **202** 응답이 3개 이벤트 POST에 미노출 → 재전송 폭주 위험 | `detections.py:~365`, `malfunctions.py:~380`, `connections.py:~350` | P1 |
| 10 | download 200이 application/json 오표기 + 400/404/**410** 미선언 | `reports.py` download 라우터 | P1 |
| 11 | `X-Client-Id` 헤더 미선언, login docstring client_id 누락 | `auth.py` login 시그니처 | P2 |
| 12 | `GET /api/grants` page/size 경계 미표기 | `grants.py:166-167` Query(ge/le) | P2 |

## [결정 필요] — 코드/명세 중 어느 쪽에 맞출지 사람이 정해야 하는 것

| # | 사안 | 선택지 | 연동 |
|---|---|---|---|
| 1 | **integrations/files 권한 모듈** | (A) `enums.py`에 2종 추가 12→14 + 그룹 매트릭스 마이그 + §9.4.7·Swagger 재덤프 / (B) permission_map 16항목을 기존 어휘(`setup_feature` 등)로 재배정 / (C) ADMIN 전용이 의도면 §9.4.7에 '매트릭스 부여 불가' 명시 | P0 §7·§9.4.7 |
| 2 | **`EnumAuditActionType` 개수** | 21종(GRANT_* 3만 추가) vs **23종**(users.py:522/:559의 raw `USER_PHOTO_CHANGED`/`USER_PHOTO_DELETED`를 enum 멤버로 승격) | §4.6 |
| 3 | **Suppression enum 배치** | (A) §4.10 신설 후 L16640 파생규칙 이관 vs (B) §4 신설 없이 §6.8 인라인 유지 + L9997 문구 1줄 수정 | §4.10/§6.8 |
| 4 | **§5.5.13 enclosure-metrics 페이징** | 명세를 코드(쿼리 0개, latest 요약)에 맞춤 vs 라우터에 페이징 구현(v4.6 M04 미해소) | P0 |
| 5 | **§6.1.1 `type_event` 쿼리** | (A) 코드에 필터 추가(malfunctions=reason과 대칭) vs (B) 명세 예시 URL 삭제 | P1 |
| 6 | **ROI/XyPoint/그룹멤버십 module 귀속** | `devices` vs `cameras` — PERMISSION_MAP 10행 등록 시 확정 필요 | P1 |
| 7 | **§10.3.4 templates 길이 제약** | 스키마 Field(min/max) 강화 vs 명세에서 '(1~100자)'·'(최대 500자)' 삭제 | P1 |
| 8 | **§6.1/§6.2 `detail` 타입** | `DetectionDetail`/`MalfunctionDetail` 적용(+extra="allow") vs "자유형 JSONB" 명기 후 미사용 클래스 2종 삭제 | P2 |
| 9 | **§6.1.3 등 POST extra 정책** | 코드 `extra="forbid"` 강화(회귀 리스크) vs 명세에 ignore 동작 명기(권고) | P2 |
| 10 | **카메라 PUT semantics** | `EventMappingCameraCreate` 재사용(필수 1필드)을 Replace 스키마로 분리할지 | P2 |
| 11 | **§8.6 시간 기준 컬럼** | 필터·정렬·삭제 기준을 `created_at` 유지(명세에 명시) vs `collected_at`으로 전환(계약 변경 → 별도 PRD) | P2 |
| 12 | **인가 서술 부록 범위** | §7.1 표 신설 단독 vs `permission_map.py` 전체(≈100항목)를 §5·§6·§7에 1:1 매핑하는 부록 도입 | P2 |
| 13 | **전역 `responses` 기본값 도입** | 공통 에러 스키마 + FastAPI 전역 등록(권장) vs 라우터별 개별 선언 유지 | P1 |

## [후속 확인] — 부분 감사 범위 밖 파생 (판정 미완)

- Camera PATCH 표 **L2830 `type_device`** — Speaker(§5.4.4)와 동일 결함 가능성, `CameraUpdate` 스키마 확인 후 §5.3.4 판정.
- 명세 **L4958**(§5.5.x Enclosure) `limit 50 + 상한 미기재` — §8.3.7과 동일 오기.
- §5.5.x Enclosure의 `before_date` 계약 vs §8.6.4 혼동 여부(복붙 흔적).
- cameras/speakers **단건 POST의 UNIQUE 위반 500** 동일 결함 여부.
- `GET /` 본문 문서화 여부 — grep 판정 곤란으로 **보류**(§13.1 부록 추가는 하되 '본문 미문서 확정' 목록에서는 제외).